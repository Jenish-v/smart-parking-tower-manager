/**
 * Deterministic parking allocation rules and in-memory allocation state.
 *
 * <p>The package owns active vehicle-to-space assignments. It consumes immutable facility configuration and does not
 * own parking-session history or persistence transactions.</p>
 */
package com.jenish.smartparking.allocation.domain;
